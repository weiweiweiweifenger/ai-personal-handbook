# AI时代个人成长说明书网站

这是一个基于静态 HTML/CSS/JS 的个人成长说明书网站原型。

## 文件

- `index.html`：页面结构与内容。
- `styles.css`：视觉样式与响应式布局。
- `script.js`：说明书章节切换交互。
- `assets/personal-os-hero.png`：首屏视觉资产。

## 使用

直接用浏览器打开 `index.html` 即可预览。后续如果拿到飞书文档正文，可以把 `index.html` 与 `script.js` 里的章节内容替换为原文结构。
